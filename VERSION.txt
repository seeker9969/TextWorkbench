2026-07-12 build — fixed a real bug: nudge left/right/up/down now correctly uses the underlying ShapeString's face-aligned orientation as the direction reference, even when you've selected the Extrusion or Fusion instead (previously those moved along the wrong axes and could push text off/into the face). Also fixed a related double-rotation bug I introduced while fixing the first one. Added a new Rotate 90° button that spins text around the face's own normal, staying flush against the surface.
