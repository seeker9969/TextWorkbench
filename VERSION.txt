2026-07-18 build (2) — removed dead code: NextFont, ChooseFont, and ChooseFontPanel were unregistered from the toolbar a while back but still fully wired up as commands, and depended on fc-list (a Linux-only external tool) which would silently fail on Windows/macOS if ever triggered. Fully deleted (classes, helper functions, unused icon files) ahead of submitting to the official FreeCAD Addon Index, per their cross-platform/no-unnecessary-dependencies requirements.
